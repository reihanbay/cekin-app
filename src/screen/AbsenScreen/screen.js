import * as React from 'react'
import { View, Text } from 'react-native'
import styles from './styles'

const AbsenScreen = ({ navigation }) => {

    return (
        <View style={styles.container}>
            <Text>This is Absen screen</Text>
        </View>
    )
}

export default AbsenScreen